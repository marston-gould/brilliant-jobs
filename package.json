{
  "name": "brilliant-jobs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "css:build": "npx tailwindcss -i src/input.css -o styles.css --minify",
    "css:watch": "npx tailwindcss -i src/input.css -o styles.css --watch"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "tailwindcss": "^3.4.19",
    "vite": "^6.0.0"
  }
}
